node {
    checkout scm
 DOCKER_IMAGE = 'node:19-alpine'
    docker.withRegistry('https://registry.hub.docker.com', 'dockerbuild') {

        def customImage = docker.build("patnorbs/web-app:v1.0")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}











